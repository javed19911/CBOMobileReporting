<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    >
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="8dp"
        android:padding="5dp"
        android:background="@drawable/custom_square_transparent_bg" >

        <utils.font_package.Button_Bold
            android:id="@+id/dr_workwith_save"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:layout_marginTop="2dp"
            android:textColor="@color/textColorPrimary"

            android:textAllCaps="false"
            android:textSize="19sp"
            android:background="@drawable/button_text_view_selector"
            android:text="Done" />

        <ListView
            android:id="@+id/dr_workwith_list"
            android:layout_above="@+id/dr_workwith_save"
            android:layout_width="match_parent"
            android:layout_height="0dip"
            android:layout_weight="1"
            android:layout_alignParentTop="true"
            >
        </ListView>

    </RelativeLayout>
</LinearLayout>
